{
  "name": "serverless-online-game",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "infra",
    "backend",
    "frontend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^25.3.3"
  }
}
